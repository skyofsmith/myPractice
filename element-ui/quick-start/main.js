import Vue from 'vue';
import Element from 'element-ui';
Vue.use(Element, {
    size: 'small',
    zIndex: 3000
});

Vue.use(Element, {
    size: 'small',
    zIndex: 3000
});
new Vue({
    el: '#app',
    render: h => h(App)
});